services:
  db:
    image: postgres:17-alpine
    restart: always
    environment:
      POSTGRES_USER: planer
      POSTGRES_PASSWORD: planer
      POSTGRES_DB: planer
    ports:
      - 5432:5432
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
